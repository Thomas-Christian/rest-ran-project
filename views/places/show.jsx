const React = require('react')
const Default = require('./default')

function Show () {

    return (
          <Default>
              <main>
                  <h1>SHOW</h1>
              </main>
          </Default>
        )
      
}

module.exports = Show
